"""
URL configuration for competitions app
"""
from django.urls import path
from rest_framework.routers import DefaultRouter

router = DefaultRouter()
# Competition endpoints will be added here

urlpatterns = [
    # path('', include(router.urls)),
]

