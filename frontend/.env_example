# =============================================================================
# CCIS-CodeHub Frontend Environment Variables
# =============================================================================

# API Configuration
VITE_API_URL=http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000/api
VITE_WS_URL=ws://localhost:8000/ws

# Firebase Configuration
# Get these from Firebase Console: https://console.firebase.google.com/
# Go to Project Settings > General > Your apps > Web app
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-firebase-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
VITE_FIREBASE_APP_ID=your-firebase-app-id
VITE_FIREBASE_MEASUREMENT_ID=G-your-measurement-id

# GitHub OAuth Configuration
VITE_GITHUB_CLIENT_ID=your-github-client-id

# Google OAuth Configuration (via Firebase)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Application Configuration
VITE_APP_NAME=CCIS CodeHub
VITE_APP_VERSION=1.0.0
VITE_APP_DESCRIPTION=AI-Driven Learning Platform for SNSU CCIS Students

# Feature Flags
VITE_ENABLE_AI_MENTOR=true
VITE_ENABLE_CODE_EDITOR=true
VITE_ENABLE_COMPETITIONS=true
VITE_ENABLE_PROJECTS=true
VITE_ENABLE_COMMUNITY=true
VITE_ENABLE_LEARNING_PATHS=true
VITE_ENABLE_ANALYTICS=true


# AI Mentor Configuration
VITE_AI_MODEL_DEFAULT=gemini
VITE_AI_MODELS_AVAILABLE=gemini,openai

# Development Tools
VITE_ENABLE_DEBUG=true
VITE_ENABLE_DEV_TOOLS=true
VITE_LOG_LEVEL=debug

# Rate Limiting (requests per minute)
VITE_API_RATE_LIMIT=60

# Upload Limits
VITE_MAX_FILE_SIZE=5242880  # 5MB in bytes
VITE_MAX_IMAGE_SIZE=2097152  # 2MB in bytes
VITE_ALLOWED_FILE_TYPES=.pdf,.doc,.docx,.txt,.py,.js,.ts,.tsx,.jsx,.java,.cpp,.c,.html,.css

# WebSocket Configuration
VITE_WS_RECONNECT_ATTEMPTS=5
VITE_WS_RECONNECT_INTERVAL=3000  # milliseconds

# Theme Configuration
VITE_DEFAULT_THEME=dark
VITE_ENABLE_THEME_TOGGLE=true

# Pagination
VITE_DEFAULT_PAGE_SIZE=20
VITE_MAX_PAGE_SIZE=100

# Cache Configuration (in seconds)
VITE_CACHE_TTL_SHORT=300      # 5 minutes
VITE_CACHE_TTL_MEDIUM=1800    # 30 minutes
VITE_CACHE_TTL_LONG=3600      # 1 hour

# CDN Configuration (optional)
VITE_CDN_URL=
VITE_IMAGE_CDN_URL=

# Analytics (optional)
VITE_GOOGLE_ANALYTICS_ID=
VITE_MIXPANEL_TOKEN=

# Sentry Error Tracking (optional)
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development

# =============================================================================
# FREE SERVICE SETUP INSTRUCTIONS
# =============================================================================

# 1. FIREBASE SETUP (FREE)
#    a. Go to: https://console.firebase.google.com/
#    b. Create a new project or select existing
#    c. Click on "Web" icon to add a web app
#    d. Register your app with a nickname
#    e. Copy the Firebase SDK config values to the variables above
#    f. Enable Authentication in Firebase Console
#    g. Enable Google Sign-In provider under Authentication > Sign-in method

# 2. GITHUB OAUTH SETUP (FREE)
#    a. Go to: https://github.com/settings/developers
#    b. Click "New OAuth App"
#    c. Set Homepage URL to: http://localhost:3000
#    d. Set Authorization callback URL to: http://localhost:3000/auth/github/callback
#    e. Copy the Client ID to VITE_GITHUB_CLIENT_ID

# 3. GOOGLE OAUTH (via Firebase - FREE)
#    a. Already configured when you set up Firebase Authentication
#    b. Get the Web client ID from Firebase Console > Authentication > Sign-in method > Google

# =============================================================================
# QUICK START INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env
#    cp .env_example .env

# 2. For local development, minimum required variables:
#    - VITE_API_URL (default: http://localhost:8000)
#    - VITE_API_BASE_URL (default: http://localhost:8000/api)
#    - VITE_WS_URL (default: ws://localhost:8000/ws)

# 3. For authentication features:
#    - Set up Firebase and add all VITE_FIREBASE_* variables
#    - Configure GitHub OAuth for social login

# 4. Optional features:
#    - Set feature flags to false to disable specific modules
#    - Configure analytics and error tracking for production

# =============================================================================
# IMPORTANT NOTES
# =============================================================================

# - All frontend environment variables must start with VITE_ to be accessible
# - These variables are embedded in the build, so they're visible to users
# - Never put sensitive secrets (like private keys) in frontend environment variables
# - For production, update URLs to use HTTPS and your actual domain
# - WebSocket URL should use wss:// protocol in production (secure WebSocket)

# =============================================================================
# PRODUCTION CONFIGURATION EXAMPLE
# =============================================================================

# For production deployment, update these values:
# VITE_API_URL=https://api.codehub.snsu.edu.ph
# VITE_API_BASE_URL=https://api.codehub.snsu.edu.ph/api
# VITE_WS_URL=wss://api.codehub.snsu.edu.ph/ws
# VITE_ENABLE_DEBUG=false
# VITE_ENABLE_DEV_TOOLS=false
# VITE_LOG_LEVEL=error
# VITE_SENTRY_ENVIRONMENT=production
